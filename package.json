{
  "name": "discipline",
  "private": true,
  "version": "1.2.1",
  "type": "module",
  "description": "Discipline 自律 —— 课程表 · 专注 · 待办 · 成就（PWA + Android APK）",
  "scripts": {
    "dev": "vite",
    "build": "npm run icons && tsc --noEmit && vite build",
    "build:gh-pages": "npm run icons && tsc --noEmit && vite build --base=/Discipline/",
    "build:html": "npm run icons && tsc --noEmit && vite build --config vite.singlefile.config.ts && node scripts/copy-html.mjs",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "icons": "node scripts/generate-icons.mjs",
    "cap:add": "npm run build && cap add android",
    "cap:sync": "npm run build && cap sync android",
    "cap:open": "cap open android",
    "deploy:gh-pages": "npm run build:gh-pages && gh-pages -d dist -b gh-pages"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/app": "^6.0.1",
    "@capacitor/core": "^6.1.2",
    "@capacitor/local-notifications": "^6.1.0",
    "@supabase/supabase-js": "^2.45.4",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.30.0",
    "recharts": "^2.13.3",
    "zustand": "^4.5.5"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2",
    "@testing-library/dom": "^10.4.0",
    "@testing-library/jest-dom": "^6.5.0",
    "@testing-library/react": "^16.0.1",
    "@types/react": "^18.3.8",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.2",
    "gh-pages": "^6.1.1",
    "jsdom": "^25.0.1",
    "netlify-cli": "^27.1.0",
    "pngjs": "^7.0.0",
    "typescript": "^5.5.4",
    "vite": "^5.4.8",
    "vite-plugin-pwa": "^0.20.5",
    "vite-plugin-singlefile": "^2.3.3",
    "vitest": "^2.1.3"
  }
}
